```mermaid
flowchart 
    U[用户]
    M[大模型]

    %% 外层包裹：文本处理阶段
    subgraph Pipeline[Tokenizer]
        
        %% 编码模块
        subgraph Encoder[编码]
            E1[切分]
            E2[映射]
        end

        %% 解码模块
        subgraph Decoder[解码]
            D1[映射]
        end

    end

    %% 流程连接
    U -- 发送消息：hello --> Encoder
    E1 --> E2
    Pipeline --> M
    M --> Decoder

```